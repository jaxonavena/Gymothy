<h1>Editing event</h1>

<%= render "form", event: @event %>

<br>

<div>
  <%= link_to "Show this event", business_event_path(@business, @event) %> |
  <%= link_to "Back to events", business_events_path(@business) %>
</div>
